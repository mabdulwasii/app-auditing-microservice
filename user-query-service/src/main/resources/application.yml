server:
  port: 8088
  
spring:
  application:
    name: user-query-service
  data:
    mongodb:
      uri: mongodb://localhost:27021/user-query

  kafka:
    transferTopic: user_topics
    bootstrapServers: localhost:9092
    groupId: group-id
    maxAttempts: 1