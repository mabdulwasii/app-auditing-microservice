server:
  port: 8089
  
spring:
  application:
    name: audit-service
  data:
    mongodb:
      uri: mongodb://localhost:27018/audit
  kafka:
    transferTopic: user_topics
    bootstrapServers: localhost:9092
    groupId: group_id_2
    maxAttempts: 1